Name:           ros-lunar-agni-tf-tools
Version:        0.1.0
Release:        1%{?dist}
Summary:        ROS agni_tf_tools package

Group:          Development/Libraries
License:        BSD
URL:            http://wiki.ros.org/agni_tf_tools
Source0:        %{name}-%{version}.tar.gz

Requires:       ros-lunar-roscpp
Requires:       ros-lunar-rviz
Requires:       ros-lunar-tf2-ros
BuildRequires:  eigen3-devel
BuildRequires:  ros-lunar-catkin
BuildRequires:  ros-lunar-roscpp
BuildRequires:  ros-lunar-rviz
BuildRequires:  ros-lunar-tf2-ros

%description
This package provides a gui program as well as a rviz plugin to publish static
transforms. Both support the transformation between various Euler angle
representations. The rviz plugin also allows to configure the transform with an
interactive marker.

%prep
%setup -q

%build
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/lunar/setup.sh" ]; then . "/opt/ros/lunar/setup.sh"; fi
mkdir -p obj-%{_target_platform} && cd obj-%{_target_platform}
%cmake .. \
        -UINCLUDE_INSTALL_DIR \
        -ULIB_INSTALL_DIR \
        -USYSCONF_INSTALL_DIR \
        -USHARE_INSTALL_PREFIX \
        -ULIB_SUFFIX \
        -DCMAKE_INSTALL_LIBDIR="lib" \
        -DCMAKE_INSTALL_PREFIX="/opt/ros/lunar" \
        -DCMAKE_PREFIX_PATH="/opt/ros/lunar" \
        -DSETUPTOOLS_DEB_LAYOUT=OFF \
        -DCATKIN_BUILD_BINARY_PACKAGE="1" \

make %{?_smp_mflags}

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/lunar/setup.sh" ]; then . "/opt/ros/lunar/setup.sh"; fi
cd obj-%{_target_platform}
make %{?_smp_mflags} install DESTDIR=%{buildroot}

%files
/opt/ros/lunar

%changelog
* Fri Mar 30 2018 Robert Haschke <rhaschke@techfak.uni-bielefeld.de> - 0.1.0-1
- Autogenerated by Bloom

* Tue Mar 20 2018 Robert Haschke <rhaschke@techfak.uni-bielefeld.de> - 0.1.0-0
- Autogenerated by Bloom

